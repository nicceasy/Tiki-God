{
  "name": "tiki-god",
  "version": "1.0.0",
  "description": "A tiki drink database, a statistical model of what makes tiki drinks work, and a generator that turns a prompt into a new recipe with its lineage.",
  "type": "module",
  "private": true,
  "scripts": {
    "validate": "node scripts/validate.mjs data/drinks/*.json",
    "merge": "node scripts/merge.mjs",
    "analyze": "node scripts/analyze.mjs",
    "build": "node scripts/merge.mjs && node scripts/analyze.mjs && node scripts/vessels-doc.mjs && node scripts/build-site.mjs",
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=20"
  }
}
